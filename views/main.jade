extends layout

block content
    .container(ng-controller="mainController as main")
        h1 Success!
        a(href="/getProfile") Click me
        p {{greeting}}
        p {{userInfo}}
        p {{user2}}
        p= user._id